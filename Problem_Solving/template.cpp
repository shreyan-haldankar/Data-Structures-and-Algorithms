#include <bits/stdc++.h>

using namespace std;

void solve()
{
    int n;cin>>n;
	vector<int>a(n,0);
	for(int i=0;i<n;i++)
		cin>>a[i];
}

int main(int argc, char const *argv[])
{
    int T;
    cin >> T;
    while(T-->0)
    {
        solve();
    }
    return 0;
}
