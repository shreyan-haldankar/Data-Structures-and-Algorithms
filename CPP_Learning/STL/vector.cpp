#include <bits/stdc++.h>

using namespace std;
int main(int argc, char const *argv[])
{
    // BASICS
    // // Vector - Dynamic sized array
    // vector<int> v; // empty vector of integers
    // // vector<int> v(10); //vector of integer with 10 elements (all 0's)
    // // vector<char> v(10,'h'); //vector of characters with 10 elements (all h's)
    // v.push_back(5);
    // v.push_back(7);
    // v.push_back(9);

    // cout << v[0] << " " << v[1] << " " << v [2] << endl;
    // // We can erase elements from the last index
    // v.pop_back();
    // v.push_back(100);
    // cout << v[0] << " " << v[1] << " " << v [2] << endl;

    // v.clear();
    // // v.pop_back() erases the last element
    // cout << v.size();
    // return 0;
    vector<int> v[50];
    // An array of 50 elements
    // Vectors can be used for adjacency list graph
    return 0;
}
